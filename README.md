# MyHome

```bash
sudo apt update -y && sudo apt upgrade -y && sudo apt-get install git -y
```

```bash
sudo git clone https://github.com/FelixBetz/DeadCoreTest.git /opt/myhome
```

```bash
cd /opt/myhome
```

```bash
bash install.sh
```
